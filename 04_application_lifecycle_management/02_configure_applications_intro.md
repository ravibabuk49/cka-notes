## 02 — Configure Applications

This section covers how application behaviour is controlled at runtime inside Kubernetes Pods. Three mechanisms are addressed:

- **Commands and Arguments** — overriding the container entrypoint and default arguments
- **Environment Variables** — injecting key-value configuration into containers
- **Secrets** — injecting sensitive configuration (passwords, tokens, keys) securely

Each is covered in detail in the sections that follow.